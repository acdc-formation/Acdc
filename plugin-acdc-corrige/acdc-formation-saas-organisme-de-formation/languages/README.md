# Traductions ACDC SAAS OF

Text Domain : `acdc-formation-saas`

Déposer ici les fichiers `.po` / `.mo` (ex. `acdc-formation-saas-fr_FR.mo`).
Le chargement est assuré par `load_plugin_textdomain()` sur le hook `init`.
