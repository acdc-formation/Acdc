{
    "name": "acdc-formation/acdc-formation-saas",
    "description": "ERP SaaS pour organisme de formation (Qualiopi) : CRM, sessions, quiz, questionnaires, dossiers, facturation, émargement, signature électronique, portails apprenant/formateur.",
    "type": "wordpress-plugin",
    "license": "GPL-2.0-or-later",
    "require": {
        "php": ">=8.2"
    },
    "require-dev": {
        "squizlabs/php_codesniffer": "^3.10",
        "wp-coding-standards/wpcs": "^3.1",
        "phpcompatibility/phpcompatibility-wp": "^2.1",
        "dealerdirect/phpcodesniffer-composer-installer": "^1.0",
        "phpstan/phpstan": "^1.12",
        "php-stubs/wordpress-stubs": "^6.6",
        "szepeviktor/phpstan-wordpress": "^1.3",
        "phpunit/phpunit": "^9.6",
        "yoast/phpunit-polyfills": "^2.0"
    },
    "autoload": {
        "psr-4": {
            "ACDC\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "ACDC\\Tests\\": "tests/"
        }
    },
    "config": {
        "allow-plugins": {
            "dealerdirect/phpcodesniffer-composer-installer": true
        },
        "sort-packages": true
    },
    "scripts": {
        "lint": "find . -name '*.php' -not -path './vendor/*' -print0 | xargs -0 -n1 -P4 php -l > /dev/null",
        "phpcs": "phpcs",
        "phpcbf": "phpcbf",
        "phpstan": "phpstan analyse --memory-limit=1G",
        "test": "phpunit",
        "check": [
            "@lint",
            "@phpcs",
            "@phpstan"
        ]
    },
    "scripts-descriptions": {
        "lint": "Vérifie la syntaxe PHP de tous les fichiers.",
        "phpcs": "Analyse le style (WordPress Coding Standards).",
        "phpcbf": "Corrige automatiquement les violations de style corrigeables.",
        "phpstan": "Analyse statique (détecte les erreurs avant l'exécution).",
        "test": "Lance la suite de tests PHPUnit.",
        "check": "Enchaîne lint + phpcs + phpstan (à utiliser en CI)."
    }
}
